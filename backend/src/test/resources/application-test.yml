# Datasource properties are injected dynamically via TestPropertyProvider.
# Security JWT secret for tests — not used in production.
micronaut:
  security:
    token:
      jwt:
        signatures:
          secret:
            generator:
              secret: "test-secret-at-least-256-bits-long-for-hs256!!"

flyway:
  enabled: false